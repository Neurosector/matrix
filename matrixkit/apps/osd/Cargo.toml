[package]
name = "matrix-osd"
version = "0.2.0"
edition.workspace = true
license.workspace = true
description = "Matrix OSD — das Tasten-Werkzeug: ausführen, Stand notieren; das Dock zeigt"

[dependencies]
matrixkit-theme = { path = "../../matrixkit-theme" }
