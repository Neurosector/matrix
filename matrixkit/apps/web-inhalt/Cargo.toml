[package]
name = "matrix-web-inhalt"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Matrix Web: der unsichtbare WebKit-Träger — UI hat nur die Kit-App"

[dependencies]
gtk = { package = "gtk4", version = "0.10" }
webkit6 = "0.5"
